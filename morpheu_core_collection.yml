plugin: morpheus_inventory
groups:
  - name: morphtest
    searchtype: label
    searchstring: whateverlabel
morpheus_url: "{{ lookup('cypher','secret=secret/my_appliance') }}"
morpheus_api_key: "{{ lookup('cypher','secret=secret/my_token_2024') }}"
